# monitor.conf — known hosts and ports for serial_monitor.sh
#
# Add entries to each list. They will appear as numbered options
# when the script runs. The first entry in each list is the default.

KNOWN_HOSTS=(
    "mata-mata@100.111.193.124"
)

KNOWN_PORTS=(
    "/dev/opencm"
    "/dev/serial/by-id/usb-CM-900_ROBOTIS_Virtual_COM_Port-if00"
    "/dev/ttyACM0"
)

BAUD="115200"
