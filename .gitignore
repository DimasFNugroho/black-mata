__pycache__/
*.pyc
.venv/
venv/
black-mata/
.pytest_cache/

# arduino-cli binary (download separately — too large for git)
bin/

# Firmware build artifacts
firmware/*/build/
*.swp
a.out
*.c

# Local development notes (not for repo)
PLAN.md
CLAUDE.md
docs/wiring/servo_map.json
cd_resume.sh

# Kinematics config file
tools/ackermann_ui/ackermann_config.json

# Runtime state (PID/log for commission_page.sh etc.)
.run/
